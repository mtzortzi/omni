[project]
name = "azure-tokens-programmatic"
version = "0.1.0"
description = "Programmatic access to Azure DevOps work items."
requires-python = ">=3.10"
dependencies = [
    "azure-devops>=7.1.0b4",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
]
